#include <stdio.h>
/**
 * main - entry point for the code
 * Description -  Prints lower cases alphabets without q and e
 * Return: 0
 */

int main(void)
{
	int a = 0;

	while (a <= 9)
	{
		putchar(a);
		a++;
	}
	putchar('\n');
	return (0);
}
