#include "main.h"
/**
 * _abs - To print the absolute value  of a number
 * @i:  Value to be checked
 * Return: n if positive, -n if negative
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
